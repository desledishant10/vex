"""Extraction attacks - training-data extraction, model fingerprinting (v0.2+).

This module is reserved for future attacks. The v0.1 system-prompt extraction
attack lives under ``agent_attacks`` because it's an agent-context probe.
"""
